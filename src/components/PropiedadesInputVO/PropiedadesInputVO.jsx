import React from "react";
import "./PropiedadesInputVO.scss";

export const PropiedadesInputVO = ({ className }) => {
  return (
    <input type="radio" className={`multiple-choice-default ${className}`} />
  );
};
